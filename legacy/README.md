# Legacy scripts

Round-specific one-off scripts (R19 etc.) kept for reference only.
Not used by the production pipeline — `generate.yml` / `publish.yml` and the
scripts in `scripts/` are the current, maintained path. Safe to delete once
you're confident you won't need to replay an old round manually.
